if [[ $(echo $OSTYPE|grep 'darwin') ]]; then
	chmod +x ./mac/install
	./mac/install
fi

chmod +x ./zsh/install
./zsh/install

# If no projects folder in home exists; make one
if [[ ! -d ~/Projects ]]; then mkdir ~/Projects; fi
